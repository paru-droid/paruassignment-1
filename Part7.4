#include <stdio.h>

//Compiler version gcc  6.3.0

int main()
{
  int n,rem=0,rev,m;
  printf("Enter a number: ");
  scanf("%d",&n);
 m=n;
  while(m>0){
    rem=m%10;
    m=m/10;
    rev=rev*10+rem;
  }
  printf("Reverse of %d is %d",n,rev);
  return 0;
}
