#include <stdio.h>

//Compiler version gcc  6.3.0

int main()
{
  char str[10],n;
  int i,c=0;
  printf("\nEnter a string");
  gets(str);
  printf("\nEnter the character which you want to count=");
  scanf("%c",&n);
  for(i=0;str[i]!='\0';i++){
    if(str[i]==n)
      
    
    c++;
 }

printf("\ntotal = %d",c);
return 0;
}
